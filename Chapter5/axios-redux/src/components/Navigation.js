import React from "react";
import Search from "./Search";

const Navigation = ({counter}) => {
   return(
    <nav>
        <h3>Nav</h3>
        <Search/>
    </nav>
   )
}

export default Navigation;